const enum Status {
  fulfillment = 'To fulfillment',
  progress = 'In progress',
  done = 'Done',
}

const enum Priority {
  low = 'low',
  middle = 'middle',
  high = 'high',
}
export { Status, Priority };
